# qa_fill_sft_checkpoint_export_wanghaonan

Merges the fill SFT step-13 LoRA checkpoint into the already merged retrieval-SFT
step-50 base. It reuses the verified Megatron worker Python and converter from the
original retrieval-SFT export.

Output:

`<fill-sft-root>/hf_export/step_13`
