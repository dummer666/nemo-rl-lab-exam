# Evidence-backed short target rebuild

Uses cached Qwen3.5-9B only as a candidate editor and independent verifier.
Every accepted point must bind to an exact `/data/docs` quote and remain fully
visible through a non-leaking one- or two-search BM25 trajectory. The job does
not train a model or modify the official reward.
