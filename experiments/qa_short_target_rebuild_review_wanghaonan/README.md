# Short target rebuild review

Reads the first successful smoke artifacts and prints every accepted target with
literal evidence quotes, visible retrieval hops, teacher/verifier outputs,
complete final answer, token audit, and a human-review checklist. It never loads
a model or modifies the artifacts.
